node_modules/
.next/
dist/
standalone/
*.log
.env.local
out/
dados/*.json
agenda/*.json
pdfs/
updates/
releases/
dist.rar

# build/: versiona só o installer.nsh (referenciado pelo electron-builder), ignora o resto
build/*
!build/installer.nsh

# cache incremental do TypeScript
tsconfig.tsbuildinfo

# temporários do Office e lixo
~$*
null

# saídas locais de análise de certificados (não versionar)
tmp_cert_extract/
tmp_test/

# quadro Check (interno/temporário): fila de demandas gerada pelo /check.html
DEMANDAS.md
check-demandas.md

# artefatos de escrita atomica (lib/dados.ts)
dados/*.bak
dados/*.tmp.*
dados/*.lock
.dev-lock
.claude/settings.local.json

# dicionário pt-BR do corretor ortográfico: gerado por scripts/copy-dicionario.js
# a partir de node_modules/dictionary-pt (ver predev/prebuild em package.json)
public/dicionario/
